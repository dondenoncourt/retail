<h2>Address Suggestions:</h2>

<g:each in="${altAddrs}" var="addr">
    ${addr.addressLine1.trim()}<br/>
    <% if (addr.addressLine2) { %>
        ${addr.addressLine2}<br/>
    <% } %>
    ${addr.city}, ${addr.state} ${addr.zip}<br/>
</g:each>
